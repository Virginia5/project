<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.maCarthy.test.mapper.TestMapper">

	<!-- <delete id="deleteTest" parameterType="java.util.Map">
		DELETE T_TEST WHERE TESTID=#{testId}
	</delete> -->

	<select id="queryTestAll" resultType="com.maCarthy.test.entity.TestEntity">
		SELECT * FROM T_TEST
	</select>
	<insert id="create" parameterType="java.util.Map" >
		insert into
		T_TEST(TESTID) values('21')
	</insert>
</mapper>